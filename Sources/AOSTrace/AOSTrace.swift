

public func A0(_ messages: String ...) {
    let msgString = messages.map{$0}.joined()
    print("\(0): \(msgString)")
}

public func A1(_ messages: String ...) {
    let msgString = messages.map{$0}.joined()
    print("\(1): \(msgString)")
}

public func A2(_ messages: String ...) {
    let msgString = messages.map{$0}.joined()
    print("\(2): \(msgString)")
}

public func A3(_ messages: String ...) {
    let msgString = messages.map{$0}.joined()
    print("\(3): \(msgString)")
}

public func A4(_ messages: String ...) {
    let msgString = messages.map{$0}.joined()
    print("\(4): \(msgString)")
}

public func A5(_ messages: String ...) {
    let msgString = messages.map{$0}.joined()
    print("\(5): \(msgString)")
}

public func A6(_ messages: String ...) {
    let msgString = messages.map{$0}.joined()
    print("\(6): \(msgString)")
}

public func A7(_ messages: String ...) {
    let msgString = messages.map{$0}.joined()
    print("\(7): \(msgString)")
}

public func A8(_ messages: String ...) {
    let msgString = messages.map{$0}.joined()
    print("\(8): \(msgString)")
}

public func A9(_ messages: String ...) {
    let msgString = messages.map{$0}.joined()
    print("\(9): \(msgString)")
}


